/* config.js — 기능 토글 & 기본 설정 (기존 유지 + 새 기능 안전 스위치) */
window.APP_CONFIG = {
  USE_OLD_UI: true,
  ENABLE_UPBIT_VERCEL_PROXY: true,
  ENABLE_BITHUMB_VERCEL_PROXY: true,
  API_BASE: "https://satoshi-wallet-2025.vercel.app",
  INTERVAL_TICKER_MS: 1500,
  INTERVAL_TOP_MS: 15000
};
