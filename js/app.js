// 검색창만 남긴 초미니멀 버전 — 의도적으로 기능 없음
document.addEventListener("DOMContentLoaded", ()=>{
  const btn = document.getElementById("search-btn");
  btn.addEventListener("click", ()=>{
    // 의도적으로 아무 것도 하지 않음
    // (나중에 다시 기능 붙일 때 여기서 시작하면 됨)
  });
});
